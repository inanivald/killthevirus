{
  "name": "kill-the-virus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "cross-env DEBUG=kill-the-virus:* nodemon ./bin/www",
    "debug": "cross-env DEBUG=kill-the-virus:* nodemon --inspect ./bin/www"
  },
  "dependencies": {
    "cross-env": "^7.0.2",
    "debug": "~2.6.9",
    "dotenv": "^8.2.0",
    "express": "~4.16.1",
    "mongoose": "^5.9.16",
    "morgan": "~1.9.1"
  },
  "engines": {
    "node": "12.x"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
